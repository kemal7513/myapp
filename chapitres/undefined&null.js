let selectedObject = {
    name: "kemal"
};
console.log(selectedObject);

selectedObject = null;

console.log(selectedObject);

//is not defined = n'est pas definit
console.log(b);

// null = c'est nous qui donnant la valeur nulle (0)
//undefined = c'est la valeur que javascript va donner par défaut quand il ne le trouve pas